# NCMS Infrastructure

Deployment and environment configuration for NCMS.

## Components
- **Docker:** Local development environment via `docker-compose`.
- **EMQX:** MQTT broker configuration and ACLs.
- **PostgreSQL:** Database schema and TimescaleDB setup.
- **Kubernetes:** Production deployment manifests.

## Local Setup
Run `docker-compose up -d` to start the infrastructure services.
